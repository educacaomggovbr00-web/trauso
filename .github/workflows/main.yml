name: Release
on:
  push:
    branches:
      - main # O build vai começar toda vez que você der push na branch main
    tags:
      - 'v*' # Também continua funcionando para quando você criar tags

jobs:
  publish-tauri:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'windows-latest'
            args: ''

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Install frontend dependencies
        run: npm install

      - name: Build frontend
        run: npm run build 

      - name: Build and Release (Tauri Action)
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: 'v${{ github.run_number }}' # Cria uma tag automática com o número do build
          releaseName: 'Trauso Build ${{ github.run_number }}'
          releaseBody: 'Build gerado automaticamente via GitHub Actions.'
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
